import { FC } from 'react'
import { motion } from 'framer-motion'

const MovieCard: FC = () => {
  return (
    <>
      <motion.div className="item rounded-xl bg-blue-600"></motion.div>
    </>
  )
}

export default MovieCard
